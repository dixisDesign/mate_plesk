{
  "routes": [
    {
      "method": "GET",
      "path": "/currents",
      "handler": "current.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/currents/count",
      "handler": "current.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/currents/:id",
      "handler": "current.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/currents",
      "handler": "current.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/currents/:id",
      "handler": "current.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/currents/:id",
      "handler": "current.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
